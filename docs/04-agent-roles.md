# Agent 角色与职责

本文档提供五个阶段的职责总览。可直接用于运行时设计的完整角色卡见 [五阶段角色卡索引](roles/README.md)。详细角色卡中的权限、流程和提示词规则优先于本总览中的简化描述。

## 1. 通用规则

每个阶段是一个业务角色，而不是固定绑定某一个模型。一个角色聊天室可以包含多个参与模型、一个协调模型和用户。

所有角色必须遵守：

- 只处理本阶段职责。
- 只使用后端授予的工具。
- 只访问项目和阶段策略允许的文件。
- 不把普通讨论冒充正式产出物。
- 发现上游问题时创建返工请求，不私自改写已批准上游文档。
- 工具权限由后端强制执行，提示词不能扩大权限。

## 2. Planner

### 目标

把用户想法转化为明确、完整且可验收的项目需求。

### 输入

- 用户初始项目目标。
- 用户在 Planner 聊天室的补充说明。
- 项目约束和已有资料。

### 主要职责

- 澄清目标用户和使用场景。
- 定义功能需求和非功能需求。
- 划分 MVP 和后续范围。
- 定义验收标准。
- 识别依赖、风险和开放问题。
- 记录用户批准的正式决定。

### 正式产出

`specs/requirements.md`

必需内容：

- Project Goal
- Target Users
- Use Cases
- Functional Requirements
- Non-functional Requirements
- Constraints
- Out of Scope
- Acceptance Criteria
- Open Questions
- Decision Log

### 权限边界

- 可以读取项目背景资料。
- 可以写入 Planner 指定的需求产出路径。
- 默认不得写业务源代码。
- 默认不得执行安装、构建、提交或部署命令。为理解现有项目确需临时修改或运行时，可以创建 `CapabilityRequest`；合法申请在所有工作流模式下都必须由用户弹窗批准，并且不能用于替代 Builder 的正式实现职责。

### 通过条件

- 没有阻断性的开放问题。
- 核心需求都有可执行验收标准。
- 范围和非目标明确。
- 用户批准正式需求。

## 3. Designer

### 目标

将批准需求转换为 Builder 可以直接执行的产品、技术和交互设计。

### 输入

- Planner 生成的 `HandoffPacket`。
- 已批准需求产出物。
- 用户在 Designer 聊天室的设计反馈。

### 主要职责

- 设计整体架构和模块边界。
- 设计数据模型、API 和事件协议。
- 设计 UI 页面、状态和交互流程。
- 确定技术栈和目录结构。
- 定义错误处理和安全边界。
- 把设计拆成明确构建任务。
- 建立需求到设计的追踪关系。

### 正式产出

- `specs/design.md`
- `specs/api.md`
- `specs/data-model.md`
- `specs/build-tasks.md`

### 权限边界

- 可以读取需求交接包和批准需求文件。
- 可以写入 Designer 指定的设计产出路径。
- 不得修改批准需求。
- 默认不得编写或修改业务代码。为验证设计假设确需临时原型或命令时，可以创建 `CapabilityRequest`；合法申请必须由用户弹窗批准，且不能形成 Builder 的正式实现产物。

### 通过条件

- 每个核心需求都有对应设计。
- 模块、接口、数据流和异常流程明确。
- Builder 不需要自行猜测关键架构决定。
- 重要设计决定得到用户批准。

## 4. Builder

### 目标

严格按照批准的需求和设计实现可以构建、测试和运行的项目。

### 输入

- Designer 生成的 `HandoffPacket`。
- 批准的需求和设计产出物。
- 当前返工反馈。

### 主要职责

- 编写生产代码。
- 编写单元测试和集成测试。
- 执行构建和测试。
- 维护实现文件清单。
- 报告设计偏差和未解决问题。
- 对需要改变需求或设计的问题提出返工请求。

### 正式产出

- 项目源代码。
- 项目测试。
- `specs/build-report.md`。

构建报告必须包含：

- Implemented Requirements
- Implemented Files
- Tests Added
- Build Commands
- Test Results
- Known Limitations
- Design Deviations
- Remaining Issues

### 权限边界

- 可以读取已批准需求和设计文件。
- 可以写入代码、测试、构建配置和 Builder 报告允许路径。
- 不得修改批准需求和设计文档。
- 安装依赖、Git 提交和高风险 Shell 操作必须符合 Tool Policy；超出 Builder 默认能力时创建 CapabilityRequest 由用户弹窗批准。

### 通过条件

- 声明实现的文件真实存在。
- 项目能够构建或启动。
- 规定测试通过。
- 不存在关键占位实现。
- 未经批准的设计偏差为零。

## 5. Reviewer

### 目标

独立验证实现是否满足需求、设计、质量和安全标准。

### 输入

- Builder 生成的 `HandoffPacket`。
- 已批准需求与设计。
- 代码、测试和构建报告。

### 主要职责

- 检查需求实现完整度。
- 检查设计一致性。
- 审查代码质量和异常处理。
- 执行构建、测试和安全检查。
- 检查占位实现、敏感信息和危险行为。
- 给出明确结论和返工目标。

### 正式产出

`specs/review.md`

结论只能是：

- `PASS`
- `NEEDS_FIX`
- `FAIL`

### 权限边界

- 默认只读需求、设计、代码和测试。
- 可以运行被批准的构建、测试和只读分析命令。
- 不得直接修改业务代码。
- 不得用“顺手修复”替代独立审查。

### 返工判断

- 需求错误或缺失：返回 Planner。
- 架构、接口、数据或交互问题：返回 Designer。
- 代码、测试和构建问题：返回 Builder。
- 严重安全或根本不可接受问题：`FAIL`。

### 通过条件

- 结论为 `PASS`。
- 所有阻断问题已经关闭。
- 必要构建和测试结果可验证。
- 用户完成规定审批。

## 6. Deployer

### 目标

把已批准实现整理为清楚、可操作的部署准备方案和部署相关文件。

MVP 中的 Deployer 不直接操作真实生产环境，也不运行本地 Docker 构建、打包或部署验证。

### 输入

- Reviewer 生成的已批准 `HandoffPacket`。
- 通过审查的代码和测试。
- 项目构建与交付配置。

### 主要职责

- 生成部署计划、环境、安装、启动、停止和回滚说明。
- 生成允许的 Dockerfile、Compose、CI、脚本和部署配置草案。
- 生成版本与发布说明。
- 汇总已知问题。
- 明确标记所有未实际验证的假设和步骤。

### 正式产出

- `specs/deployment/**`。
- Stage Contract 允许的部署相关文件。

交付说明必须包含：

- Version
- Target Environment
- Prerequisites
- Configuration
- Deployment Steps
- Health Check Plan
- Logging and Troubleshooting
- Installation
- Startup Instructions
- Known Issues
- Rollback Instructions

### 权限边界

- 可以读取已批准代码和交付配置。
- 可以写入 Stage Contract 允许的部署文档和部署配置路径。
- 不得修改产品功能和业务逻辑。
- 发现代码或构建配置问题时返回 Builder。
- 第一版不得访问线上生产环境，也不能通过权限申请开启真实部署。

### 通过条件

- Reviewer 结论为 `PASS`。
- 必需部署文档和声明的部署文件真实存在。
- 安装、启动、停止、健康检查和回滚说明完整。
- 不含真实凭据，未验证内容明确标记。

## 7. 用户角色

用户不是普通 Agent。用户具有以下能力：

- 参与所有聊天室。
- 配置和启停模型。
- 确认需求和重要设计决定。
- 批准、驳回或要求修改阶段结果。
- 处理 CapabilityRequest，并批准或拒绝超出角色默认能力的操作。
- 暂停、恢复和停止工作流。
- 查看完整审计记录。

用户操作同样必须记录时间、目标对象和决定内容。

## 8. 阶段内多 Agent 模式

每个聊天室可以包含：

- 多个参与模型（`Participant`）：分别提出方案、风险和改进意见。
- 一个协调模型（`Coordinator`）：综合参与提案和用户意见，生成共识草案。
- 用户（`Human Participant`）：提出要求并作出正式决定。

协调模型的输出仍然只是阶段草案。只有写入正式产出物并通过质量门后，才能作为交接内容。
